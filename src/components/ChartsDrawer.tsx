// Deprecated component; replaced by MermaidDrawer and wc singleton. Keeping file for reference would confuse imports.
// Provide a minimal stub that delegates to MermaidDrawer to avoid breaking imports until all references are updated.
import MermaidDrawer from './MermaidDrawer'
export default MermaidDrawer
